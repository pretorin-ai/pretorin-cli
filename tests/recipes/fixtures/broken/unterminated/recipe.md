---
id: unterminated
version: 0.1.0
name: "Unterminated"
description: "This recipe never terminates its frontmatter block."

This body should never be reached because the frontmatter has no closing delimiter.
